<!-- MARMOTA-GENCODE: VERSION 2.0 - 11/04/2014 09:42:28 AM -->
<Window
    x:Class="Systemas.Vista.VistaSysperfiusuario"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:i="http://schemas.microsoft.com/expression/2010/interactivity"
    xmlns:cmd="clr-namespace:GalaSoft.MvvmLight.Command;assembly=GalaSoft.MvvmLight.Extras.WPF45"
    mc:Ignorable="d"
    WindowStartupLocation="CenterScreen"
    DataContext="{Binding Path=VistaModeloSysperfiusuario, Source={StaticResource SYS_Locator}}"
    Title="Maestro de Perfiles del Sistema"
    Icon="/Sistema;component/Imagenes/sis_logo40.png"
    Width="900"
    Height="578"
    AllowsTransparency="True"
    Background="Transparent"
    WindowStyle="None"
    ResizeMode="NoResize"
    ShowInTaskbar="True">
    <Window.Resources>
        <ResourceDictionary >
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary
              	Source= "/Sistema;component/Skins/EstiloSistema.xaml">
                </ResourceDictionary>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>
    <Grid Margin="0,0,0,0">
        <Grid.ColumnDefinitions>
            <ColumnDefinition />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="14" />
            <RowDefinition Height="42" />
            <RowDefinition Height="6*" />
            <RowDefinition Height="7*" />
            <RowDefinition Height="18*" />
            <RowDefinition Height="57" />
        </Grid.RowDefinitions>
        <Border Height="564"
                Width="900"
                Style="{DynamicResource styleBorderFondoForm}"
                Grid.RowSpan="5"
                Grid.Row="1">
        </Border>
        <Image HorizontalAlignment="Left"
               Height="40"
               Width="899"
               Margin="0,0,0,0"
               VerticalAlignment="Top"
               Grid.Row="1"
               Source="/Sistema;component/Imagenes/sys_barratitulo.png"
               StretchDirection="UpOnly"
               Stretch="Fill"
               MouseLeftButtonDown="Window_MouseLeftButtonDown" />
        <Image HorizontalAlignment="Left"
               Height="50"
               Margin="5,1,0,0"
               VerticalAlignment="Top"
               Width="50"
               Grid.RowSpan="2"
               Source="/Sistema;component/Imagenes/sis_logo40.png"
               MouseLeftButtonDown="Window_MouseLeftButtonDown" />
        <TextBlock Text="Maestro de Perfiles del Sistema"
                   Grid.Row="1"
                   Style="{StaticResource styleTituloForm}"
                   MouseLeftButtonDown="Window_MouseLeftButtonDown"/>
        <Image MouseDown="btnCerrar_KeyDown"
               Style="{StaticResource styleImgButtonCerrarForm}"
               Source="/Sistema;component/Imagenes/sis_cerrar40.png"
               Grid.Row="1"
               IsEnabled="{Binding Path=GlgSIS_ModoDefault}"
               Cursor="Hand"/>
        <Image HorizontalAlignment="Left"
               Height="50"
               Width="898"
               Margin="1,3,0,0"
               VerticalAlignment="Top"
               Grid.Row="5"
               Source="/Sistema;component/Imagenes/sis_fondo40.png"
               StretchDirection="UpOnly"
               Stretch="Fill"/>
        <!-- Filtro -->
        <Button Content="Filtro..."
                Height="26"
                HorizontalAlignment="Left"
                Margin="395,7,0,0"
                Name="cmdFilVista1"
                VerticalAlignment="Top"
                Width="89"
                Visibility="Hidden"
                Command="{Binding CmdFIL}" Grid.RowSpan="2"/>
        <!-- Fin Filtro-->
        <!-- Filtro -->
        <Button Content="Filtro..."
                Height="26"
                HorizontalAlignment="Left"
                Margin="395,7,0,0"
                Name="cmdFilVista2"
                VerticalAlignment="Top"
                Width="89"
                Visibility="Hidden"
                Command="{Binding CmdFILREL}" Grid.RowSpan="2"/>
        <!-- Fin Filtro-->
        <!-- Perfil Usuario -->
        <TextBox Name="lblSysUsuIdUsuario"
                 HorizontalAlignment="Left"
                 Visibility="Hidden"
                 Text="{Binding Path=GcrUsuIdUsuario,UpdateSourceTrigger=PropertyChanged}"
                 VerticalAlignment="Top"
                 Height="23"
                 Width="89" Grid.RowSpan="2"/>
        <TextBox Name="lblSysUsuCodigoPerfil"
                 HorizontalAlignment="Left"
                 Visibility="Hidden"
                 Text="{Binding Path=GcrUsuCodigoPerfil,UpdateSourceTrigger=PropertyChanged}"
                 VerticalAlignment="Top"
                 Height="23"
                 Width="89" Grid.RowSpan="2"/>
        <!-- Fin Perfil Usuario -->
        <Grid Grid.Row="2">
            <TextBlock Name="lblG1sys_codper_perf"
                               Width="137"
                               Margin="8,8,0,0"
                               ToolTip="Código único del perfil en el sistema (generado por el sistema)"
                               Grid.Row="2"
                               Style="{StaticResource styleEtiquetasForm}">
                               <Run Language="es-co" Text="Código del Perfil"/>
            </TextBlock>
            <TextBox Name="txtG1Sys_codper_perf"
                             Width="280"
                             Margin="150,8,0,0"
                             TabIndex="1"
                             Text="{Binding Path=G1Sys_codper_perf, UpdateSourceTrigger=PropertyChanged, ValidatesOnDataErrors=True}"
                             Style="{StaticResource styleTextBoxForm}"
                             IsEnabled="False"
                             KeyDown="fcvMoverFocus_KeyDown"
                             Grid.Row="2"
                             GotFocus="fcvTextBox_GotFocus"/>
            <TextBlock Name="lblG1sys_desper_perf"
                               Width="137"
                               Margin="8,31,0,0"
                               ToolTip="Descripción del perfil textual del perfil"
                               Grid.Row="2"
                               Style="{StaticResource styleEtiquetasForm}">
                               <Run Language="es-co" Text="Nombre del Perfil"/>
            </TextBlock>
            <TextBox Name="txtG1Sys_desper_perf"
                             Width="280"
                             Margin="150,31,0,0"
                             TabIndex="2"
                             Text="{Binding Path=G1Sys_desper_perf, UpdateSourceTrigger=PropertyChanged, ValidatesOnDataErrors=True}"
                             Style="{StaticResource styleTextBoxForm}"
                             IsEnabled="{Binding Path=GlgSIS_ModoEdicion}"
                             KeyDown="fcvMoverFocus_KeyDown"
                             Grid.Row="2"
                             GotFocus="fcvTextBox_GotFocus"/>
            <TextBlock Name="lblG1sys_rutimg_perf"
                               Width="137"
                               Margin="8,54,0,0"
                               ToolTip="Ruta y Nombre de la Imagen jpg que lo representa en las vista de Menú"
                               Grid.Row="2"
                               Style="{StaticResource styleEtiquetasForm}">
                               <Run Language="es-co" Text="Imagen de  Vista"/>
            </TextBlock>
            <TextBox Name="txtG1Sys_rutimg_perf"
                             Width="280"
                             Margin="150,54,0,0"
                             TabIndex="3"
                             Text="{Binding Path=G1Sys_rutimg_perf, UpdateSourceTrigger=PropertyChanged, ValidatesOnDataErrors=True}"
                             Style="{StaticResource styleTextBoxForm}"
                             IsEnabled="False"
                             KeyDown="fcvMoverFocus_KeyDown"
                             Grid.Row="2"
                             GotFocus="fcvTextBox_GotFocus"/>
            <ComboBox Name="cboG1Sys_nivusu_perf"
                    	Width="207"
                    	Margin="650,8,0,0"
                    	Grid.Row="2"
                    	IsEnabled="{Binding Path=GlgSIS_ModoEdicion}"
                    	Style="{StaticResource styleComboBoxForm}"
                    	SelectedIndex="0"
                    	SelectionChanged="SeleccionComboBoxOpcion"
                    	ItemsSource="{Binding Path=G1CbSys_nivusu_perf}"
                    	SelectedValuePath="ValorSeleccion">
                <ComboBox.ItemTemplate>
                    <DataTemplate>
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="{Binding Path=NombreOpcion}"/>
                        </StackPanel>
                    </DataTemplate>
                </ComboBox.ItemTemplate>
            </ComboBox>
            <TextBlock Name="lblG1sys_nivusu_perf"
                               Width="137"
                               Margin="435,8,0,0"
                               ToolTip="Nivel del usuario en el sistema: 1=Súper usuario 2=Administrador 3=Usuario de gestión"
                               Grid.Row="2"
                               Style="{StaticResource styleEtiquetasForm}">
                               <Run Language="es-co" Text="Nivel del Usuario"/>
            </TextBlock>
            <TextBox Name="txtG1Sys_nivusu_perf"
                             Width="70"
                             Margin="577,8,0,0"
                             TabIndex="4"
                             Text="{Binding Path=G1Sys_nivusu_perf, UpdateSourceTrigger=PropertyChanged, ValidatesOnDataErrors=True}"
                             Style="{StaticResource styleTextBoxForm}"
                             TextChanged="ActualizarComboBoxOpcion"
                             IsEnabled="{Binding Path=GlgSIS_ModoEdicion}"
                             KeyDown="fcvMoverFocus_KeyDown"
                             Grid.Row="2"
                             GotFocus="fcvTextBox_GotFocus"/>
            <ComboBox Name="cboG1Sys_estper_perf"
                    	Width="207"
                    	Margin="650,31,0,0"
                    	Grid.Row="2"
                    	IsEnabled="{Binding Path=GlgSIS_ModoEdicion}"
                    	Style="{StaticResource styleComboBoxForm}"
                    	SelectedIndex="0"
                    	SelectionChanged="SeleccionComboBoxOpcion"
                    	ItemsSource="{Binding Path=G1CbSys_estper_perf}"
                    	SelectedValuePath="ValorSeleccion">
                <ComboBox.ItemTemplate>
                    <DataTemplate>
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="{Binding Path=NombreOpcion}"/>
                        </StackPanel>
                    </DataTemplate>
                </ComboBox.ItemTemplate>
            </ComboBox>
            <TextBlock Name="lblG1sys_estper_perf"
                               Width="137"
                               Margin="435,31,0,0"
                               ToolTip="Estado del Perfil: 1=Activo 2=Inactivo"
                               Grid.Row="2"
                               Style="{StaticResource styleEtiquetasForm}">
                               <Run Language="es-co" Text="Estado del perfil"/>
            </TextBlock>
            <TextBox Name="txtG1Sys_estper_perf"
                             Width="70"
                             Margin="577,31,0,0"
                             TabIndex="5"
                             Text="{Binding Path=G1Sys_estper_perf, UpdateSourceTrigger=PropertyChanged, ValidatesOnDataErrors=True}"
                             Style="{StaticResource styleTextBoxForm}"
                             TextChanged="ActualizarComboBoxOpcion"
                             IsEnabled="{Binding Path=GlgSIS_ModoEdicion}"
                             KeyDown="fcvMoverFocus_KeyDown"
                             Grid.Row="2"
                             GotFocus="fcvTextBox_GotFocus"/>
        </Grid>
        <Grid Grid.Row="3">
            <TextBlock Name="lblG2sys_codreg_cper"
                               Width="137"
                               Margin="8,8,0,0"
                               ToolTip="Código único del registro generado por el sistema"
                               Grid.Row="3"
                               Style="{StaticResource styleEtiquetasForm}">
                               <Run Language="es-co" Text="Código registro"/>
            </TextBlock>
            <TextBox Name="txtG2Sys_codreg_cper"
                             Width="280"
                             Margin="150,8,0,0"
                             TabIndex="6"
                             Text="{Binding Path=G2Sys_codreg_cper, UpdateSourceTrigger=PropertyChanged, ValidatesOnDataErrors=True}"
                             Style="{StaticResource styleTextBoxForm}"
                             IsEnabled="False"
                             KeyDown="fcvMoverFocus_KeyDown"
                             Grid.Row="3"
                             GotFocus="fcvTextBox_GotFocus"/>
            <TextBlock Name="lblG2sys_codcom_comd"
                               Width="137"
                               Margin="8,31,0,0"
                               ToolTip="Código Único del componente en asignación en módulos (código de asignación)"
                               Grid.Row="3"
                               Style="{StaticResource styleEtiquetasForm}">
                               <Run Language="es-co" Text="Código asignación en Módulo"/>
            </TextBlock>
            <TextBox Name="txtG2Sys_codcom_comd"
                             Width="280"
                             Margin="150,31,0,0"
                             TabIndex="7"
                             Text="{Binding Path=G2Sys_codcom_comd, UpdateSourceTrigger=PropertyChanged, ValidatesOnDataErrors=True}"
                             Style="{StaticResource styleTextBoxForm}"
                             IsEnabled="{Binding Path=GlgSIS_ModoEdicion}"
                             KeyDown="txtG2Sys_codcom_comd_KeyDown"
                             Grid.Row="3"
                             GotFocus="fcvTextBox_GotFocus"/>
            <TextBox Name="txtG2Sys_nommod_modu"
                             Width="207"
                             Margin="223,54,0,0"
                             TabIndex="108"
                             Text="{Binding Path=G2Sys_nommod_modu, UpdateSourceTrigger=PropertyChanged, ValidatesOnDataErrors=True}"
                             Style="{StaticResource styleTextBoxForm}"
                             IsEnabled="False"
                             KeyDown="fcvMoverFocus_KeyDown"
                             Grid.Row="3"
                             GotFocus="fcvTextBox_GotFocus"/>
            <TextBlock Name="lblG2sys_codmod_modu"
                               Width="137"
                               Margin="8,54,0,0"
                               ToolTip="Código único del Módulo al cual se asocia el componente asignado al perfil"
                               Grid.Row="3"
                               Style="{StaticResource styleEtiquetasForm}">
                               <Run Language="es-co" Text="Código Módulo"/>
            </TextBlock>
            <TextBox Name="txtG2Sys_codmod_modu"
                             Width="70"
                             Margin="150,54,0,0"
                             TabIndex="8"
                             Text="{Binding Path=G2Sys_codmod_modu, UpdateSourceTrigger=PropertyChanged, ValidatesOnDataErrors=True}"
                             Style="{StaticResource styleTextBoxForm}"
                             IsEnabled="{Binding Path=GlgSIS_ModoEdicion}"
                             KeyDown="txtG2Sys_codmod_modu_KeyDown"
                             Grid.Row="3"
                             GotFocus="fcvTextBox_GotFocus"/>
            <TextBlock Name="lblG2sys_llavco_cper"
                               Width="137"
                               Margin="8,77,0,0"
                               ToolTip="Llave de verificación  es el Código Módulo + Código único del Componente (SYS_CODMOD_MODU+SYS_CODCOM_COMP) ejm:  FCMCOM0015 donde FCM y COM0015 son Módulo y componente"
                               Grid.Row="3"
                               Style="{StaticResource styleEtiquetasForm}">
                               <Run Language="es-co" Text="Llave verificación"/>
            </TextBlock>
            <TextBox Name="txtG2Sys_llavco_cper"
                             Width="280"
                             Margin="150,77,0,0"
                             TabIndex="9"
                             Text="{Binding Path=G2Sys_llavco_cper, UpdateSourceTrigger=PropertyChanged, ValidatesOnDataErrors=True}"
                             Style="{StaticResource styleTextBoxForm}"
                             IsEnabled="{Binding Path=GlgSIS_ModoEdicion}"
                             KeyDown="fcvMoverFocus_KeyDown"
                             Grid.Row="3"
                             GotFocus="fcvTextBox_GotFocus"/>
            <TextBox Name="txtG2Sys_titcom_comp"
                             Width="207"
                             Margin="650,8,0,0"
                             TabIndex="110"
                             Text="{Binding Path=G2Sys_titcom_comp, UpdateSourceTrigger=PropertyChanged, ValidatesOnDataErrors=True}"
                             Style="{StaticResource styleTextBoxForm}"
                             IsEnabled="False"
                             KeyDown="fcvMoverFocus_KeyDown"
                             Grid.Row="3"
                             GotFocus="fcvTextBox_GotFocus"/>
            <TextBlock Name="lblG2sys_codcom_comp"
                               Width="137"
                               Margin="435,8,0,0"
                               ToolTip="Código Único del componente (Formulario, opción reporte y otros)  asociado al Módulo ejm: COM0015"
                               Grid.Row="3"
                               Style="{StaticResource styleEtiquetasForm}">
                               <Run Language="es-co" Text="Código componente"/>
            </TextBlock>
            <TextBox Name="txtG2Sys_codcom_comp"
                             Width="70"
                             Margin="577,8,0,0"
                             TabIndex="10"
                             Text="{Binding Path=G2Sys_codcom_comp, UpdateSourceTrigger=PropertyChanged, ValidatesOnDataErrors=True}"
                             Style="{StaticResource styleTextBoxForm}"
                             IsEnabled="{Binding Path=GlgSIS_ModoEdicion}"
                             KeyDown="txtG2Sys_codcom_comp_KeyDown"
                             Grid.Row="3"
                             GotFocus="fcvTextBox_GotFocus"/>
            <TextBlock Name="lblG2sys_prmetr_comp"
                               Width="137"
                               Margin="435,31,0,0"
                               ToolTip="Expresión de texto que se agregan como parámetros en los casos que sean requeridos"
                               Grid.Row="3"
                               Style="{StaticResource styleEtiquetasForm}">
                               <Run Language="es-co" Text="Parámetros"/>
            </TextBlock>
            <TextBox Name="txtG2Sys_prmetr_comp"
                             Width="280"
                             Margin="577,31,0,0"
                             TabIndex="11"
                             Text="{Binding Path=G2Sys_prmetr_comp, UpdateSourceTrigger=PropertyChanged, ValidatesOnDataErrors=True}"
                             Style="{StaticResource styleTextBoxForm}"
                             IsEnabled="{Binding Path=GlgSIS_ModoEdicion}"
                             KeyDown="fcvMoverFocus_KeyDown"
                             Grid.Row="3"
                             GotFocus="fcvTextBox_GotFocus"/>
            <ComboBox Name="cboG2Sys_estccp_cper"
                    	Width="207"
                    	Margin="650,54,0,0"
                    	Grid.Row="3"
                    	IsEnabled="False"
                    	Style="{StaticResource styleComboBoxForm}"
                    	SelectedIndex="0"
                    	SelectionChanged="SeleccionComboBoxOpcion"
                    	ItemsSource="{Binding Path=G2CbSys_estccp_cper}"
                    	SelectedValuePath="ValorSeleccion">
                <ComboBox.ItemTemplate>
                    <DataTemplate>
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="{Binding Path=NombreOpcion}"/>
                        </StackPanel>
                    </DataTemplate>
                </ComboBox.ItemTemplate>
            </ComboBox>
            <TextBlock Name="lblG2sys_estccp_cper"
                               Width="137"
                               Margin="435,54,0,0"
                               ToolTip="Estado del Componente  dentro del perfil  1= Activo 2= Inactivo"
                               Grid.Row="3"
                               Style="{StaticResource styleEtiquetasForm}">
                               <Run Language="es-co" Text="Estado componente"/>
            </TextBlock>
            <TextBox Name="txtG2Sys_estccp_cper"
                             Width="70"
                             Margin="577,54,0,0"
                             TabIndex="12"
                             Text="{Binding Path=G2Sys_estccp_cper, UpdateSourceTrigger=PropertyChanged, ValidatesOnDataErrors=True}"
                             Style="{StaticResource styleTextBoxForm}"
                             TextChanged="ActualizarComboBoxOpcion"
                             IsEnabled="False"
                             KeyDown="fcvMoverFocus_KeyDown"
                             Grid.Row="3"
                             GotFocus="fcvTextBox_GotFocus"/>
            <TextBlock Name="lblG2sys_desper_perf"
                               Width="137"
                               Margin="435,77,0,0"
                               ToolTip="Descripción del perfil textual del perfil"
                               Grid.Row="3"
                               Style="{StaticResource styleEtiquetasForm}">
                               <Run Language="es-co" Text="Nombre del Perfil"/>
            </TextBlock>
            <TextBox Name="txtG2Sys_desper_perf"
                             Width="280"
                             Margin="577,77,0,0"
                             TabIndex="13"
                             Text="{Binding Path=G2Sys_desper_perf, UpdateSourceTrigger=PropertyChanged, ValidatesOnDataErrors=True}"
                             Style="{StaticResource styleTextBoxForm}"
                             IsEnabled="{Binding Path=GlgSIS_ModoEdicion}"
                             KeyDown="fcvMoverFocus_KeyDown"
                             Grid.Row="3"
                             GotFocus="fcvTextBox_GotFocus"/>
        </Grid>
        <Grid Grid.Row="4">
            <Rectangle HorizontalAlignment="Left"
               VerticalAlignment="Top"
               Height="235"
               Width="897"
               Grid.Row="4"
               OpacityMask="{DynamicResource {x:Static SystemColors.ActiveBorderBrushKey}}"
               Fill="#FFF6F8F9"
               Margin="1,0,0,0"/>
            <Button Name="cmdAddAux"
                    Content="Guardar"
                    Margin="585,6,0,0"
                    Style="{StaticResource styleButtonMiniForm}"
                    TabIndex="14"
                    Command="{Binding CmdSAVREL}"
                    Click="fcvGuardarRegistroRel"
                    Grid.Row="4"
                    ToolTip="Adicionar registro en la Grid"/>
            <Button Name="cmdCanAux"
                    Content="Nuevo"
                    Margin="662,6,0,0"
                    Style="{StaticResource styleButtonMiniForm}"
                    TabIndex="80"
                    Command="{Binding CmdCANREL}"
                    Click="fcvNuevoRegistroRel"
                    Grid.Row="4"
                    ToolTip="Nuevo o deshacer cambios en registro modificado"/>
            <Button Name="cmdDelAux"
                    Content="Eliminar"
                    Margin="739,6,0,0"
                    Style="{StaticResource styleButtonMiniForm}"
                    TabIndex="81"
                    Command="{Binding CmdDELREL}"
                    Click="fcvEliminarRegistroRel"
                    Grid.Row="4"
                    ToolTip="Eliminar registro activo de la lista"/>

            <!-- PRUEBA-->
            <Button Name="cmdPrueba"
                    Content="Generar"
                    Margin="14,6,0,0"
                    Style="{StaticResource styleButtonMiniForm}"
                    TabIndex="14"
                    ToolTip="Generar la Factura" Click="cmdPrueba_Click"/>

            <Button Name="cmdPrueba1"
                    Content="Firmar"
                    Margin="92,6,0,0"
                    Style="{StaticResource styleButtonMiniForm}"
                    TabIndex="14"
                    ToolTip="Firmar" Click="cmdPruebaFirmar_Click"/>

            <Button Name="cmdPrueba2"
                    Content="Enviar"
                    Margin="170,6,0,0"
                    Style="{StaticResource styleButtonMiniForm}"
                    TabIndex="14"
                    ToolTip="Adicionar registro en la Grid" Click="cmdPruebaEnviar_Click"/>

            <Button Name="cmdPrueba3"
                    Content="Consultar Estado"
                    Margin="248,6,0,0"
                    Style="{StaticResource styleButtonMiniForm}"
                    TabIndex="14"
                    ToolTip="Consultar estado validacion factura" Click="cmdPruebaConsultar_Click" Width="103"/>

            <Button Name="cmdPrueba4"
                    Content="Consultar factura radicada"
                    Margin="359,6,0,0"
                    Style="{StaticResource styleButtonMiniForm}"
                    TabIndex="14"
                    ToolTip="Consultar factura radicada y aceptada" Click="cmdPruebaRadicado_Click" Width="140"/>

            <Button Name="cmdPrueba5"
                    Content="llave tencnica producción"
                    Margin="713,47,0,0"
                    Style="{StaticResource styleButtonMiniForm}"
                    TabIndex="14"
                    ToolTip="Consultar llave tencnica de producción" Click="CmdllaveTecnicaProducc_Click" Width="148"/>


            <DataGrid AutoGenerateColumns="False"
                      HorizontalAlignment="Left"
                      VerticalAlignment="Top"
                      Margin="12,113,0,-12"
                      Name="objDataGrid"
                      ItemsSource="{Binding TmpG2ListaBrow, UpdateSourceTrigger=PropertyChanged}"
                      CanUserAddRows="False"
                      Height="169"
                      Width="878"
                      TabIndex="100">
                <DataGrid.Columns>
                    <DataGridTextColumn Binding="{Binding Sys_codper_perf}"
                                        Header="Código del Perfil"
                                        Width="80"
                                        IsReadOnly="True">
                    </DataGridTextColumn>
                    <DataGridTextColumn Binding="{Binding Sys_desper_perf}"
                                        Header="Nombre del Perfil"
                                        Width="100"
                                        IsReadOnly="True" />
                    <DataGridTextColumn Binding="{Binding Sys_codcom_comd}"
                                        Header="Código asignación en Módulo"
                                        Width="80"
                                        IsReadOnly="True">
                    </DataGridTextColumn>
                    <DataGridTextColumn Binding="{Binding Sys_codmod_modu}"
                                        Header="Código Módulo"
                                        Width="80"
                                        IsReadOnly="True">
                    </DataGridTextColumn>
                    <DataGridTextColumn Binding="{Binding Sys_nommod_modu}"
                                        Header="Nombre Módulo"
                                        Width="100"
                                        IsReadOnly="True" />
                    <DataGridTextColumn Binding="{Binding Sys_llavco_cper}"
                                        Header="Llave verificación"
                                        Width="80"
                                        IsReadOnly="True">
                    </DataGridTextColumn>
                    <DataGridTextColumn Binding="{Binding Sys_codcom_comp}"
                                        Header="Código componente"
                                        Width="80"
                                        IsReadOnly="True">
                    </DataGridTextColumn>
                    <DataGridTextColumn Binding="{Binding Sys_titcom_comp}"
                                        Header="Titulo del componente"
                                        Width="100"
                                        IsReadOnly="True" />
                    <DataGridTextColumn Binding="{Binding Sys_prmetr_comp}"
                                        Header="Parámetros"
                                        Width="80"
                                        IsReadOnly="True">
                    </DataGridTextColumn>
                    <DataGridTextColumn Binding="{Binding Sys_estccp_cper}"
                                        Header="Estado componente"
                                        Width="80"
                                        IsReadOnly="True">
                    </DataGridTextColumn>
                </DataGrid.Columns>
                <i:Interaction.Triggers>
                    <i:EventTrigger
            		    EventName="SelectionChanged">
                        <cmd:EventToCommand
            			    Command="{Binding SelectionChangedCommand, Mode=TwoWay}"
            			    CommandParameter="{Binding SelectedItem, ElementName=objDataGrid}" />
                    </i:EventTrigger>
                </i:Interaction.Triggers>
            </DataGrid>
            <TextBox Name="txtNumeroFactura" HorizontalAlignment="Left" Text="SETP990000004" Height="19" Margin="14,37,0,0" TextWrapping="Wrap" VerticalAlignment="Top" Width="148"/>
            <TextBox Name="txtSecuencial" HorizontalAlignment="Left" Text="5" Height="19" Margin="182,37,0,0" TextWrapping="Wrap" VerticalAlignment="Top" Width="99"/>
            <TextBox Name="txtTipoDocumento" HorizontalAlignment="Left" Text="ND" Height="19" Margin="303,37,0,0" TextWrapping="Wrap" VerticalAlignment="Top" Width="163"/>
            <TextBox Name="txtIDTestSetPrueba" HorizontalAlignment="Left" Height="19" Margin="14,63,0,0" TextWrapping="Wrap" VerticalAlignment="Top" Width="452"/>
            <TextBox Name="txtIDRadicadoFactura" HorizontalAlignment="Left" Height="19" Margin="14,87,0,0" TextWrapping="Wrap" VerticalAlignment="Top" Width="847"/>
            <TextBox Name="txtNit" HorizontalAlignment="Left" Text="Escriba el Nit.." Height="19" Margin="533,52,0,0" TextWrapping="Wrap" VerticalAlignment="Top" Width="163"/>
        </Grid>
        <Button Name="cmdGuardar"
                Margin="300,10,0,0"
                Style="{StaticResource styleButtonImgForm}"
                TabIndex="16"
                Command="{Binding CmdSAV}"
                Click="fcvGuardarRegistro"
                Grid.Row="5"
                ToolTip="Haga clic aqui para guardar los cambios">
            <StackPanel Orientation="Horizontal"
                        Style="{StaticResource styleStackPanelBotonesForm}">
                <Image x:Name="imgGuardar"
                           Source="/Sistema;component/Imagenes/sis_edt_guardar.png"
                           Style="{StaticResource styleImagBotonesForm}"
                           Stretch="UniformToFill"/>
                <TextBlock x:Name="lblGuardar"
                               Text="Guardar"
            	               Style="{StaticResource styleLblBotonesForm}"/>
            </StackPanel>
        </Button>
        <Button Name="cmdAdicionar"
                Margin="300,10,0,0"
                Style="{StaticResource styleButtonImgForm}"
                TabIndex="17"
                Command="{Binding CmdADD}"
                Click="fcvNuevoRegistro"
                Grid.Row="5"
                ToolTip="Haga clic aqui para crear un nuevo registro">
            <StackPanel Orientation="Horizontal"
                        Style="{StaticResource styleStackPanelBotonesForm}">
                <Image x:Name="imgAdicionar"
                           Source="/Sistema;component/Imagenes/sis_edt_adicionar.png"
                           Style="{StaticResource styleImagBotonesForm}"
                           Stretch="UniformToFill"/>
                <TextBlock x:Name="lblAdicionar"
                               Text="Nuevo"
            	               Style="{StaticResource styleLblBotonesForm}"/>
            </StackPanel>
        </Button>
        <Button Name="cmdCancelar"
                Margin="403,10,0,0"
                Style="{StaticResource styleButtonImgForm}"
                TabIndex="18"
                Command="{Binding CmdCAN}"
                Click="fcvCancelarRegistro"
                Grid.Row="5"
                ToolTip="Cancelar los cambios">
            <StackPanel Orientation="Horizontal"
                        Style="{StaticResource styleStackPanelBotonesForm}">
                <Image x:Name="imgCancelar"
                           Source="/Sistema;component/Imagenes/sis_edt_cancelar.png"
                           Style="{StaticResource styleImagBotonesForm}"
                           Stretch="UniformToFill"/>
                <TextBlock x:Name="lblCancelar"
                               Text="Cancelar"
            	               Style="{StaticResource styleLblBotonesForm}"/>
            </StackPanel>
        </Button>
        <Button Name="cmdModificar"
                Margin="403,10,0,0"
                Style="{StaticResource styleButtonImgForm}"
                TabIndex="19"
                Command="{Binding CmdEDT}"
                Click="fcvModificarRegistro"
                Grid.Row="5"
                ToolTip="Haga clic aqui para modificar el registro activo">
            <StackPanel Orientation="Horizontal"
                        Style="{StaticResource styleStackPanelBotonesForm}">
                <Image x:Name="imgModificar"
                           Source="/Sistema;component/Imagenes/sis_edt_modificar.png"
                           Style="{StaticResource styleImagBotonesForm}"
                           Stretch="UniformToFill"/>
                <TextBlock x:Name="lblModificar"
                               Text="Modificar"
            	               Style="{StaticResource styleLblBotonesForm}"/>
            </StackPanel>
        </Button>
        <Button Name="cmdEliminar"
                Margin="506,10,0,0"
                Style="{StaticResource styleButtonImgForm}"
                TabIndex="20"
                Command="{Binding CmdDEL}"
                Grid.Row="5"
                ToolTip="Eliminar este registro">
            <StackPanel Orientation="Horizontal"
                        Style="{StaticResource styleStackPanelBotonesForm}">
                <Image x:Name="imgEliminar"
                           Source="/Sistema;component/Imagenes/sis_edt_eliminar.png"
                           Style="{StaticResource styleImagBotonesForm}"
                           Stretch="UniformToFill"/>
                <TextBlock x:Name="lblEliminar"
                               Text="Eliminar"
            	               Style="{StaticResource styleLblBotonesForm}"/>
            </StackPanel>
        </Button>
        <Button Name="cmdSalir"
                Margin="780,10,0,0"
                Style="{StaticResource styleButtonImgForm}"
                TabIndex="21"
                Command="{Binding CmdSAL}"
                Click="cmdSalir_Click"
                Grid.Row="5"
                ToolTip="Salir de esta pantalla">
            <StackPanel Orientation="Horizontal"
                        Style="{StaticResource styleStackPanelBotonesForm}">
                <Image x:Name="imgSalir"
                           Source="/Sistema;component/Imagenes/sis_edt_salir.png"
                           Style="{StaticResource styleImagBotonesForm}"
                           Stretch="UniformToFill"/>
                <TextBlock x:Name="lblSalir"
                               Text="Salir"
            	               Style="{StaticResource styleLblBotonesForm}"/>
            </StackPanel>
        </Button>
        <Button Name="cmdBrowser"
                Margin="18,10,0,0"
                Style="{StaticResource styleButtonImgForm}"
                TabIndex="15"
                Command="{Binding CmdDFL}"
                Click="fcvBrowserBuscar"
                Grid.Row="5"
                ToolTip="Haga clic aqui para buscar un registro">
            <StackPanel Orientation="Horizontal"
                        Style="{StaticResource styleStackPanelBotonesForm}">
                <Image x:Name="imgBrowser"
                           Source="/Sistema;component/Imagenes/sis_edt_buscar.png"
                           Style="{StaticResource styleImagBotonesForm}"
                           Stretch="UniformToFill"/>
                <TextBlock x:Name="lblBrowser"
                               Text="Buscar..."
            	               Style="{StaticResource styleLblBotonesForm}"/>
            </StackPanel>
        </Button>
    </Grid>
</Window>
