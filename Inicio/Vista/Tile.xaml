<UserControl x:Class="Inicio.Vista.Tile"
            xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
	        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
	        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
	        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
	        mc:Ignorable="d"
	        x:Name="UserControl"
	        d:DesignWidth="640" 
            d:DesignHeight="480" 
            Width="130" 
            Height="170" 
            Opacity="1" 
            PreviewMouseDoubleClick="Tile_PreviewMouseDoubleClick" Cursor="Hand">
            <UserControl.Resources>
                <Storyboard x:Key="OnMouseEnter1">
                    <DoubleAnimationUsingKeyFrames BeginTime="00:00:00" Storyboard.TargetName="TileSurfaceRct" Storyboard.TargetProperty="(UIElement.Opacity)">
                        <SplineDoubleKeyFrame KeyTime="00:00:00" Value="0"/>
                        <SplineDoubleKeyFrame KeyTime="00:00:00.3000000" Value="0.14"/>
                    </DoubleAnimationUsingKeyFrames>
                </Storyboard>
                <Storyboard x:Key="OnMouseLeave1">
                    <DoubleAnimationUsingKeyFrames BeginTime="00:00:00" Storyboard.TargetName="TileSurfaceRct" Storyboard.TargetProperty="(UIElement.Opacity)">
                        <SplineDoubleKeyFrame KeyTime="00:00:00" Value="0.14"/>
                        <SplineDoubleKeyFrame KeyTime="00:00:00.3000000" Value="0"/>
                    </DoubleAnimationUsingKeyFrames>
                </Storyboard>
            </UserControl.Resources>
            <UserControl.Triggers>
                <EventTrigger RoutedEvent="Mouse.MouseEnter">
                    <BeginStoryboard Storyboard="{StaticResource OnMouseEnter1}"/>
                </EventTrigger>
                <EventTrigger RoutedEvent="Mouse.MouseLeave">
                    <BeginStoryboard Storyboard="{StaticResource OnMouseLeave1}" x:Name="OnMouseLeave1_BeginStoryboard"/>
                </EventTrigger>
            </UserControl.Triggers>

    <Grid x:Name="LayoutRoot" >
        <Image HorizontalAlignment="Stretch" 
               x:Name="TileIcon" 
               VerticalAlignment="Stretch" 
               Width="130"             
               Height="170" 
               Margin="0,0,0,0" 
               RenderTransformOrigin="0.0,0.0" />
        <Image x:Name="Icono"             
               Height="60" 
               Width="80"
               Margin="24,25,20,75" />
        <!-- Titulo del Modulo -->
        <TextBlock HorizontalAlignment="Center" 
                   Margin="10,100,10,10" 
                   x:Name="TileTxtBlck" 
                   FontSize="12"
                   FontFamily="Segoe UI Symbol"
                   TextAlignment="Center"
                   Foreground="#FFFFFFFF" 
                   Text="Midwssoft Galeno V4.0" 
                   Width="110"
                   TextWrapping="Wrap" />
        <!-- Capa trasparente sobre el tile -->
        <Rectangle HorizontalAlignment="Center" 
                   x:Name="TileSurfaceRct" 
                   Fill="#FF000000" 
                   Opacity="0" 
                   Width="118" 
                   Height="135"  
                   Stroke="#FFFBFBFB" 
                   Grid.ColumnSpan="2"/>
    </Grid>
</UserControl>
